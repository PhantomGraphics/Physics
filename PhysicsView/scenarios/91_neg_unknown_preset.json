{
  "name": "91_neg_unknown_preset",
  "steps": [
    { "label": "Unknown rigid-body preset name is rejected (Phase 0 fix -- previously silently fell back to ScenePreset::Custom, see docs/todo/PLAN_physics_scenario_test_rebuild.md 1.4)",
      "command": "SetPreset:NoSuchRigidPreset", "expect_prefix": "Error:" },
    { "label": "A real preset name still works after the rejected one",
      "command": "SetPreset:SphereDrop", "expect": "OK" },
    { "label": "Explicit Custom is still accepted as a real, named preset (not a fallback)",
      "command": "SetPreset:Custom", "expect": "OK" },
    { "label": "Unknown soft-body preset name is rejected (SoftBodyPreset has no Custom escape hatch at all)",
      "command": "SetPreset:NoSuchSoftPreset", "expect_prefix": "Error:" },
    { "label": "A real soft-body preset still works after the rejected one",
      "command": "SetPreset:JellyDrop", "expect": "OK" }
  ]
}
